# CLAUDE.md — Kai Marketing OS

Claude Code reads this file; the canonical agent context lives in `AGENTS.md` (single source of truth for all agents). At session start, also read `memory/MEMORY.md`. Load-on-demand detail is in `.claude/rules/`.

@AGENTS.md
